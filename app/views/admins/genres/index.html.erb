<div>
  <%= form_for(@genre, url: admins_genres_path) do |f| %>
   <h2>ジャンル</h2>
   <%= f.text_field :name %>
   <%= f.submit '追加', class: "btn btn-primary col-xs-10" %>
  <% end %>
</div>

<div>
  <% @genres.each do |genre| %>
  <%= genre.name %>
  <% end %>
</div>


